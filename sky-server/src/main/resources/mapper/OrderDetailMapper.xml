<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">


    <insert id="insertBatch">
        insert into order_detail (order_id, dish_id, setmeal_id, name, dish_flavor, number, amount, image)
        values
        <foreach collection="orderDetailList" item="orderDetail" separator=",">
            (#{orderDetail.orderId}, #{orderDetail.dishId}, #{orderDetail.setmealId}, #{orderDetail.name},
             #{orderDetail.dishFlavor}, #{orderDetail.number}, #{orderDetail.amount}, #{orderDetail.image})
        </foreach>
    </insert>

    <select id="getByOrderId" resultType="com.sky.entity.OrderDetail">
        select * from order_detail where order_id = #{orderId}
    </select>

    <select id="getDishTop" resultType="java.util.Map">
        select name,sum(od.number) as number  from order_detail od left join orders o on
        od.order_id = o.id where o.status = 5 and o.order_time between #{begin} and #{end}
        group by name order by number desc
    </select>

    <select id="getDishTop2" resultType="com.sky.dto.GoodsSalesDTO">
        select name,sum(od.number) as number  from order_detail od left join orders o on
            od.order_id = o.id where o.status = 5 and o.order_time between #{beginTime} and #{endTime}
        group by name order by number desc limit 0,10
    </select>

</mapper>
